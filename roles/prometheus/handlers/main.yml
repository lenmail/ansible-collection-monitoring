---
- name: restart prometheus
  ansible.builtin.systemd:
    daemon_reload: true
    name: prometheus
    state: restarted
  become: true
  tags: prometheus

- name: reload prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: reloaded
  become: true
  tags: prometheus
