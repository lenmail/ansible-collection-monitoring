---
- name: restart alloy
  systemd:
    name: "{{ alloy_service_name }}"
    state: restarted
    daemon_reload: true
  become: true

- name: reload alloy
  systemd:
    name: "{{ alloy_service_name }}"
    state: reloaded
  become: true

- name: start alloy
  systemd:
    name: "{{ alloy_service_name }}"
    state: started
    enabled: "{{ alloy_service_enabled }}"
  become: true
